<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lukas.app.mapper.ActivityMapper">

	<resultMap id="activityResultMap" type="com.lukas.app.domain.Activity">
		<id column="id" property="id" />
		<result column="user_id" property="userId" />
		<result column="type" property="type" />
		<result column="details" property="details" />
		<result column="created_at" property="createdAt" />
	</resultMap>
	
	<insert id="insertActivity" parameterType="com.lukas.app.domain.Activity">
		INSERT INTO activities (user_id, type, details, created_at)
		VALUES (#{userId}, #{type}, #{details}, #{createdAt})
	</insert>
	
	<select id="findByUserId" resultMap="activityResultMap">
		SELECT id, user_id, type, details, created_at
		FROM activities
		WHERE user_id = #{userId}
		ORDER BY created_at DESC
	</select>
	
	<select id="findRecentByUserId" resultMap="activityResultMap">
		SELECT id, user_id, type, details, created_at
		FROM activities
		WHERE user_id = #{userId}
		ORDER BY created_at DESC
		LIMIT #{limit}
	</select>
</mapper>